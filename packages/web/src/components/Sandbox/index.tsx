import React from 'react';
import { SandboxText, SandboxWrapper } from './styles';

export const Sandbox: React.FC = () => (
  <SandboxWrapper>
    <SandboxText>Shiba UI</SandboxText>
  </SandboxWrapper>
);
